---
title: "インフラ編: 自分だけの Twin:te をサーバで動かそう"
description: "自分の PC で動かした Twin:te を、クラウド上のサーバに引っ越して動かします。本番と同じ「ビルド済みイメージを pull する」方式を体験します。"
---

このページは**インフラ編**の入り口です。前提は「PC とターミナルが使えること」だけで、Web 開発の経験は問いません。

## この編のゴール

Web アプリはコードを書いて終わりではありません。どこかのコンピュータの上で**動かし続けて**、初めてサービスになります。インフラ編のテーマは、この「Twin:te を**どこで・どうやって**動かすか」です。

やることは大きく2段階です。

1. **自分の PC で動かす** — まずは手元で Twin:te を丸ごと立ち上げます
2. **サーバで動かす** — 同じものを、インターネット上のサーバ(クラウドの VM)に引っ越します

2段階目が終わると、クラウド上に**あなた専用の Twin:te インスタンス**が誕生します。

Twin:te の本番環境は Akamai Cloud(旧 Linode。以下、管理画面の名残りで Linode と呼びます[^akamai])の上で動いています。今日は同じ Akamai Cloud に、あなた用のサーバを1台立てます。

[^akamai]: Linode は老舗の VPS(仮想サーバ)事業者で、2022年に CDN 大手の Akamai に買収されて「Akamai Cloud」になりました。管理画面の URL が `cloud.linode.com` だったり、VM のことを「Linode」と呼んだりと、あちこちに Linode の名前が残っています。

## 本番の全体像

まず、Twin:te の本番環境の全体像を見てみましょう。実物の構成図が、インフラ管理リポジトリの [twinte-infra-v4 の `docs/infrastructure.drawio.svg`](https://github.com/twin-te/twinte-infra-v4/blob/master/docs/infrastructure.drawio.svg) にあります。

登場人物を整理すると、

- ユーザーからのアクセスは Cloudflare を経由して Akamai Cloud に届く
- Akamai Cloud 上には **staging(検証用)と production(本番用)の2つの環境**があり、それぞれ **app サーバと db サーバの2台**で構成されている(合計4台)
- 各環境のサーバ同士は **VPC** というプライベートネットワークでつながっている
- 各サーバの前には **Firewall** が置かれていて、許可した通信しか通さない

という構成です。

今日はこの**縮小版**を作ります。具体的には、**1台の VM に app も db も全部入り**という構成です。それでも「本番と同じクラウドのサーバで、本番と同じ方式でアプリを動かす」ところまでやります。

## 進め方

上から順に進めてください。

1. [自分の PC で Twin:te を動かそう](/infra/pc/) — まずは手元で一式を起動します
2. [サーバを用意しよう](/infra/server/) — SSH 鍵を作り、Web UI で VM とファイアウォールを作ります
3. [サーバで Twin:te を動かそう](/infra/deploy/) — ビルド済みイメージを pull して起動し、手元のブラウザから繋ぎます
4. [後片付けの儀式](/infra/cleanup/) — **一番大事なステップです**

:::caution[お金の話を最初にします]
クラウドの VM は**起動している時間だけ課金**されます。今回使うプラン(Linode 4GB)は1時間あたり数円ですが、**消し忘れて1ヶ月放置すると数千円**になります。この演習は最後の「後片付けの儀式」までがワンセットです。途中で帰る場合も、必ずインスタンスを削除してから帰ってください。
:::

## アカウントの準備

Akamai Cloud のアカウントは、運営が管理しているチームアカウントに**制限付きユーザー**としてあなたを招待します。招待メールが届いていない人は運営に声をかけてください。

招待を受けたら [Akamai Cloud Manager](https://cloud.linode.com/) にログインできることを確認しましょう。

<!-- TODO: screenshot (Cloud Manager ログイン後のダッシュボード) -->

準備ができたら、[自分の PC で Twin:te を動かす](/infra/pc/)ところから始めましょう!
